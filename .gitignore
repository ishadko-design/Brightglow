# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Local secrets (real tokens live here; never commit)
Secrets.xcconfig

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (if ever used)
Pods/
Carthage/Build/

# Claude Code local settings (may contain tokens)
.claude/

# Separate product, own git repo
leadbridge/

# Node / Deno tooling artifacts (pricing-engine/, supabase/functions type-checking)
node_modules/
deno.lock
pricing-engine/dist/

# Raw hi-res card masters (5–16MB each). The site ships the optimized
# derivatives in media/slides and media/biz/hero, generated from these — the
# masters themselves must not be committed or deployed.
site/media/biz/cards/
